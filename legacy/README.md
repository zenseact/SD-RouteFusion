# Legacy Development Artifacts

This directory contains internal development adapters and cluster scripts that
were used while building the route-generation toolkit. They are retained for
traceability, but they are not part of the public SD-RouteFusion submission
workflow.

Use the public `GenericJSONL` and `nuPlan` adapters documented in the repository
root README for reproducible route-prior generation.
